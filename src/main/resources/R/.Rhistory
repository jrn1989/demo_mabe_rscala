library(readr)
rawData <- read_csv("rawData.csv")
View(rawData)
View(rawData)
ajusta<-function(df){df$Prediccion <- df$Anio+10 return(df)}
ajusta<-function(df){df$Prediccion <- df$Anio+10
return(df)}
modelo <- ajusta(rawData)
View(modelo)
View(modelo)
rnorm(8)
ajusta<-function(x){rawData$Prediccion <- rawData$Anio+x
return(rawData)}
ajusta(5)
filtraAparato<-function(df,cadena){
if(cadena!="Todos")
df<-df[which(df$Aparato==cadena),]
else
df
}
x<-filtraAparato(modelo,"TV")
View(x)
View(modelo)
x<-filtraAparato(modelo,"Todos")
filtraAparato<-function(cadena){
df<-mod
if(cadena!="Todos")
df<-df[which(df$Aparato==cadena),]
else
df
}
filtraAparato("TV")
filtraAparato<-function(cadena){
df<-modelo
if(cadena!="Todos")
df<-df[which(df$Aparato==cadena),]
else
df
}
filtraAparato("TV")
y<-filtraAparato("TV")
View(y)
filtraAnio<-function(numero){
df<-df[which(df$Anio==numero),]
}
y<-filtraAnio(2017)
filtraAparato<-function(cadena,numero){
df<-modelo
if(cadena!="Todos")
df<-df[which(df$Aparato==cadena),]
else
df
df<-filtraAnio(df,numero)
}
filtraAnio<-function(df,numero){
df<-df[which(df$Anio==numero),]
}
y<-filtraAparato("TV",2016)
